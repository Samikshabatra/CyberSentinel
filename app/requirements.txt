# Dependencies for the Streamlit Community Cloud deployment.
#
# Community Cloud accepts a dependency file next to the entrypoint, which keeps
# this separate from the root requirements.txt that builds the Vercel API.
#
# The console talks to the deployed API, but it imports the package directly
# and falls back to calling the service layer in-process when the API is
# unreachable, so the base runtime is listed too. The ML extras are not: the
# hosted app never loads model weights.
streamlit>=1.49
pandas>=2.2
plotly>=5.22
pydantic>=2.7
pydantic-settings>=2.2
python-dotenv>=1.0
pyyaml>=6.0
httpx>=0.27
sqlalchemy>=2.0
langgraph>=0.2.28
langchain-core>=0.3.0
